# Public FRONTEND App Key. Empty runs explicitly labelled offline defaults.
# Never use the C# backend key or management credentials in distributed clients.
TOGGLY_APP_KEY=
# Environment names are case-sensitive and must exist in your application.
TOGGLY_ENVIRONMENT=Production
# Optional current-user cache directory. No filesystem cache unless set.
TOGGLY_SNAPSHOT_DIRECTORY=
