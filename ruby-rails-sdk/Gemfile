source "https://rubygems.org"
gem "rails", "8.1.3.1"
gem "toggly-rails", "0.2.1"
gem "toggly", "0.5.0"
gem "puma"
gem "propshaft"

# Rails 8.1 JSON decoding passes positional options; JSON 3 requires keywords.
# Keep the latest stable Rails/SDK while resolving its compatible JSON API.
gem "json", ">= 2", "< 4"
