# Copy to .env and source it with set -a; these are server-only values.
TOGGLY_APP_KEY=
TOGGLY_ENVIRONMENT=Production
TOGGLY_REFRESH_INTERVAL=10
# Never put an app key in browser JavaScript or source control.
# Generate SECRET_KEY_BASE for a real production launch (see README).
