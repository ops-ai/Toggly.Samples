name: Hono sample
on:
  push:
    branches: [develop, main]
    paths: ['nodejs-hono-sdk/**', '.github/workflows/hono.yml', 'README.md', 'docs/**']
  pull_request:
    branches: [develop, main]
    paths: ['nodejs-hono-sdk/**', '.github/workflows/hono.yml', 'README.md', 'docs/**']
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: hono-${{ github.ref }}
  cancel-in-progress: true
jobs:
  nodejs-hono-sdk:
    name: nodejs-hono-sdk
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: nodejs-hono-sdk
    env:
      TOGGLY_APP_KEY: ci-placeholder
      TOGGLY_ENVIRONMENT: Production
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: nodejs-hono-sdk/package-lock.json
      - run: npm ci
      - run: npm run build
      - run: npm test
