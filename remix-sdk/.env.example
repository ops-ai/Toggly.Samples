# Server-only key used by loader/action examples. Leave blank to explore safely.
TOGGLY_APP_KEY=
# Browser-exposed App Key for RemixTogglyProvider. It is compiled into browser assets.
REMIX_PUBLIC_TOGGLY_APP_KEY=
# Toggly environment. Production is the documented fallback when omitted.
TOGGLY_ENVIRONMENT=Production
