import { json, type LoaderFunctionArgs } from '@remix-run/node'
import { Link, useLoaderData } from '@remix-run/react'
import { ORDERS } from '../lib/sample-data'
import { hasServerKey, requestContext, sampleLoader } from '../lib/toggly.server'
export async function loader({ request }: LoaderFunctionArgs) { const id = new URL(request.url).searchParams.get('id') || 'ord-vip'; const order = ORDERS.find((entry) => entry.id === id) || ORDERS[0]; if (!hasServerKey()) return json({ order, enabled: false, configured: false }); const client = (await sampleLoader()).getClient(); client.registerContext('Order', (value: typeof order) => ({ kind: 'Order', key: value.id, attributes: { Vip: value.vip, Total: value.total } })); await client.init(); return json({ order, enabled: await client.isEnabled('ExpressCheckout', requestContext(request), false, order, 'Order'), configured: true }) }
export default function Orders() { const data = useLoaderData<typeof loader>(); return <><h1>Order VIP context</h1><p><code>ExpressCheckout</code> must be configured with ContextProperty <code>Order.Vip=true</code>. Mapping happens once; each evaluation passes its own Order entity.</p>{ORDERS.map((order) => <p key={order.id}><Link to={`?id=${order.id}`}>{order.id}</Link> — Vip={String(order.vip)}, total={order.total}</p>)}<pre>{JSON.stringify(data, null, 2)}</pre><p>Without an Order or with the wrong context kind, an entity gate fails closed. A high order total does not imply VIP.</p></> }
