# Copy this file to ignored .env.local. The Electron MAIN process reads it.
# This is an App Key, which selects public feature definitions for one app. It
# is not a management API credential and is never placed in the renderer bundle.
TOGGLY_APP_KEY=
# Match the environment configured in the Toggly dashboard exactly.
TOGGLY_ENVIRONMENT=Production
