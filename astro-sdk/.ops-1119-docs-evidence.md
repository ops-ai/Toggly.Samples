# OPS-1119 temporary docs mismatch evidence

Internal builder evidence only — not customer-facing. Delete or ignore after
the Docs slice consumes it.

## Published package vs Toggly Docs `sdks/astro.mdx`

Pinned sample package: `@ops-ai/astro-feature-flags-toggly@1.15.0`
(peer `astro: ^5 \|\| ^6 \|\| ^7`). Docs guide path checked:
`Toggly Docs/sdks/astro.mdx` (workspace copy at scaffold time).

| Topic | Docs / assumption | Published 1.15.0 | Sample action |
| --- | --- | --- | --- |
| Client store path | Some older material / drafts assumed `/client` `togglyStore` | Exports are `/client/store` and `/client/setup` only | Use real paths; README lists them |
| Middleware import | Docs show `createTogglyMiddleware` from package root | Also exported from `/integration`; root re-exports it | Sample imports from `/integration` (same as middleware factory home) |
| Groups/claims at runtime | Easy to misread as fully mutable | Docs correctly note groups/claims are init-time; `setIdentity` updates identity only | Server uses per-request middleware config; README states the gap |
| HTTP filters (country/UA/lang/device/OS) | Filter template expects matrix presets | Server `buildEvalContext` has no request UA/country/language fields | Honest **unsupported** matrix rows; no forged headers |
| `@nanostores/svelte` | Peer dependency listed | Package 404 on npm registry; peer marked optional | Do not install; Svelte helpers still work via shared store |
| Astro 7 peer | Historical Wave 2 note: peer was `^5` only | 1.15.0 peers `^5 \|\| ^6 \|\| ^7` | Prerequisite resolved; clean install without force |
| Feature off path | Prefer negate | Docs and `Feature.astro` use `negate` | Sample uses negate, no FeatureOff |
| Variant API | getVariant with enableVariants | Confirmed on `TogglyServer` + client store | Separate request-scoped client with `enableVariants: true` |
| `evaluateGate` + `flagDefaults` | Easy to assume Feature.astro honors defaults offline | Without `enableVariants`, `evaluateGate` reads only `definitions` (empty in defaults-only mode) → false; `getFlag` still falls back to defaults. With `enableVariants`, boolean defaults work but entity context is ignored on that path | Middleware/integration set `enableVariants: true` so Feature.astro boolean gates work offline; Order/entity checks use a separate `enableVariants: false` client; documented in README gaps |
| Feature.astro + entity offline | Docs show `context` / `contextKind` | Same evaluateGate split as above | Live App Key + definition mode required for ContextProperty Feature gates; sample shows programmatic entity `getFlag` |

## Install proof commands (scaffold)

```text
npm view @ops-ai/astro-feature-flags-toggly version  → 1.15.0
npm view astro version  → 7.3.2
```

Re-run `npm ls` inside `astro-sdk/` after lockfile generation for exact tree.
