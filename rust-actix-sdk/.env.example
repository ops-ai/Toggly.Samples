# Export these values in your shell; this app does not load dotenv files.
# Empty or placeholder key selects an explicitly labelled signed loopback fixture.
TOGGLY_APP_KEY=
TOGGLY_ENVIRONMENT=Production
# Optional bind override. Defaults are loopback 127.0.0.1:8018.
# ACTIX_PORT=8018
# Generate per local run if you want cookie persistence across restarts.
# SAMPLE_COOKIE_KEY=
